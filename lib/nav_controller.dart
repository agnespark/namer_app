import 'package:get/get.dart';

import 'nav_model.dart';

class NavController extends GetxController {
  // static NavController get to => Get.find<NavController>();

  // RxList<Menu> data = <Menu>[].obs;
  // RxList<Menu> get data => _data;

  // RxList<Menu> convertedMenuList =
  //     menuList.map<Menu>((item) => Menu.fromJson(item)).toList().obs;

  // @override
  // void onInit() {
  //   super.onInit();
  //   menuList.forEach((element) {
  //     data.add(Menu.fromJson(element));
  //   });
  //   print(convertedMenuList);
  //   print(convertedMenuList[1].subMenu);
  // }
}
